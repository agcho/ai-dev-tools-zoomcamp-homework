{
    "name": "coding-interview-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm --prefix frontend run dev\" \"python -m uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000\"",
        "test": "pytest -q"
    },
    "devDependencies": {
        "concurrently": "8.2.0"
    }
}